# 설치 가이드 — "기기 보호를 위해 앱 차단됨"(Play Protect) 우회

사이드로드(스토어 밖) APK는 Google Play Protect가 검사하며, 접근성·마이크 등 민감 권한을
쓰는 앱은 설치 시 **"기기 보호를 위해 앱이 차단되었습니다"** 가 뜰 수 있다. 이는 앱 결함이
아니라 미등록 개발자 사이드로드에 대한 기본 경고다. 아래 중 하나로 설치한다.

## 방법 A — 그냥 설치 (가장 간단)
1. 차단 팝업에서 **"자세히"** 또는 **"More details"** 탭.
2. **"무시하고 설치"** / **"Install anyway"** 선택.
   - 버튼이 안 보이면: 같은 팝업의 **"앱 세부정보 보기"** 후 다시 설치.

## 방법 B — ADB로 설치 (차단 팝업 자체를 회피, 권장)
PC에서 USB 디버깅을 켠 뒤:
```
adb install -r PhoneAgent-1.1.2-release.apk
```
ADB 설치는 Play Protect의 설치 차단 흐름을 대부분 거치지 않는다.

## 방법 C — Play Protect 검사 일시 해제
설치하는 동안만 잠깐 끄고, 끝나면 다시 켠다.
1. **Play 스토어 → 프로필 아이콘 → Play Protect → 설정(톱니)**.
2. **"Play Protect로 앱 검사"** 끄기 → APK 설치 → 다시 켜기.

## 설치 후 — 접근성이 "제한된 설정"으로 회색일 때 (Android 13+)
사이드로드 앱은 접근성 토글이 잠겨 있을 수 있다.
1. **설정 → 앱 → PhoneAgent** 이동.
2. 우측 상단 **⋮ (점 3개) → "제한된 설정 허용"** / **"Allow restricted settings"** 탭.
3. 그 후 **설정 → 접근성 → PhoneAgent → 사용** 토글이 활성화된다.

## 왜 이 빌드가 덜 차단되나 (v1.1.2)
- 강한 차단 트리거였던 **`QUERY_ALL_PACKAGES` 권한을 제거**하고, 정책 친화적인
  `<queries>`(런처 인텐트) 방식으로 설치 앱을 조회하도록 변경했다. 기능(open_app)은 동일.

## 근본적으로 경고를 없애려면
- 자체 서명 사이드로드는 본질적으로 "미확인 개발자"라 경고가 날 수 있다. 완전히 없애려면
  Google Play(내부 테스트 트랙 포함) 또는 다른 신뢰 스토어를 통해 배포해야 한다. 개인
  사용 목적이면 위 A/B 방법으로 충분하다.
